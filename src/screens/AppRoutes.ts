/**
 * Keys of registered routes.
 * Values used in the URL. Use lower-case with hyphens for spaces.
 */
export enum AppRoutes {
    Home = 'home',
    Temp = 'temp',
    ApiTemp = 'api-temp',
    Decks = 'decks',
}
export default AppRoutes;
